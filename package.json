{
  "name": "insta-webyar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "test": "node --test tests/polyweight/*.test.mjs && wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "hono": "^4.5.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.4.5",
    "wrangler": "^4.47.0"
  }
}
