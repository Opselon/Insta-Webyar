{
  "name": "insta-webyar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "test": "wrangler deploy --dry-run --outdir dist && echo 'No automated tests yet.'"
  },
  "dependencies": {
    "hono": "^4.5.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.80.3"
  }
}
